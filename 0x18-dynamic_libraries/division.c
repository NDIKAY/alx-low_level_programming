#include <stdio.h>
#include <stdlib.h>

/**
 * div - division function of two numbers
 * @a: first number
 * @b: second number
 * Return: sum of two numbers
 */

int divi(int a, int b)
{
	return (a / b);
}

