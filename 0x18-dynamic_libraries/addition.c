#include <stdio.h>
#include <stdlib.h>

/**
 * add - addition function of two numbers
 * @a: first number
 * @b: second number
 * Return: sum of two numbers
 */

int add(int a, int b)
{
	return (a + b);
}

