#include <stdio.h>
#include <stdlib.h>

/**
 * addition - addition function of two numbers
 * @a: first number
 * @b: second number
 * Return: sum of two numbers
 */

int addition(int a, int b)
{
	return (a + b);
}

