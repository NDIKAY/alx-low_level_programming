#include <stdio.h>
#include <stdlib.h>

/**
 * sub - substraction function of two numbers
 * @a: first number
 * @b: second number
 * Return: sub of two numbers
 */

int sub(int a, int b)
{
	return (a - b);
}

