#include <stdio.h>
#include <stdlib.h>

/**
 * substraction - substraction function of two numbers
 * @a: first number
 * @b: second number
 * Return: sub of two numbers
 */

int substraction(int a, int b)
{
	return (a - b);
}

