#include <stdio.h>
#include <stdlib.h>

/**
 * multiplication - multipl function of two numbers
 * @a: first number
 * @b: second number
 * Return: multiplication of two numbers
 */

int multiplication(int a, int b)
{
	return (a * b);
}

