#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multipl function of two numbers
 * @a: first number
 * @b: second number
 * Return: multiplication of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}

