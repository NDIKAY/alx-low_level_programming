#include <stdio.h>
#include <stdlib.h>

/**
 * mod - modulo function of two numbers
 * @a: first number
 * @b: second number
 * Return: mod of two numbers
 */

int mod(int a, int b)
{
	return (a % b);
}

