#include <stdio.h>
#include <stdlib.h>

/**
 * modulo - modulo function of two numbers
 * @a: first number
 * @b: second number
 * Return: mod of two numbers
 */

int modulo(int a, int b)
{
	return (a % b);
}

